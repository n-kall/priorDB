---
title: Poisson model
---

Model:

$$
\begin{align}
y_i \sim \text{Poisson}(\exp{\mu_i}) \\
\mu_i = \alpha + \beta \cdot x_i
\end{align}
$$

Parameters needing priors:

- $\alpha$ (intercept)
- $\beta$ (predictor weights)

Data:

- $y$ (continuous outcome)
- $x$ (predictors)


